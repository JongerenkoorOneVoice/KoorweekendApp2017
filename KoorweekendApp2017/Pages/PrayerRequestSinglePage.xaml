<?xml version="1.0" encoding="UTF-8"?>
<ContentPage
		xmlns="http://xamarin.com/schemas/2014/forms"
		xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
		x:Class="KoorweekendApp2017.Pages.PrayerRequestSinglePage"
		Title="{Binding Title}">
	<ContentPage.Content>
		<RelativeLayout
			VerticalOptions="FillAndExpand"
			HorizontalOptions="FillAndExpand"
			>
      		<Image
				Source="PageBackground.jpg"
				VerticalOptions="StartAndExpand"
				HorizontalOptions="StartAndExpand"
				Aspect="Fill"
			/>
			<ScrollView
				Orientation = "Vertical"
				VerticalOptions="StartAndExpand"
				HorizontalOptions="FillAndExpand"
				x:Name="scrollView"
				>
				<StackLayout Padding="15,15,15,15">
					<Label Text="Voor" Font="Bold"></Label>
					<Label Text="{Binding Detail}"></Label>
					<Label Text="Titel" Font="Bold" Margin="0, 15, 0, 0"></Label>
					<Label Text="{Binding Title}"></Label>
					<Label Text="Omschrijving" Font="Bold" Margin="0, 15, 0, 0"></Label>
					<Label Text="{Binding Text}"></Label>
				</StackLayout>
			</ScrollView>
		</RelativeLayout>
	</ContentPage.Content>
</ContentPage>
